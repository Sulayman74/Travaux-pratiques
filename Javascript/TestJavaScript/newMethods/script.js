let mesNbrs = [2, 4, 6, 8, 10];
let index = 0;
let newArr = mesNbrs.map((element) => {
  if (element > 5) {
    index++;
    return element;
    c;
  } else {
    index++;
    return (element =
      "je suis plus petit que 5 " + " mon index est: " + (index - 1));
  }
});
// console.log(index);
console.log(newArr);
